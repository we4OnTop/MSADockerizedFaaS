[project]
name = "orchestration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "docker>=7.1.0",
    "flask>=3.1.2",
    "pyasyncore>=1.0.5",
    "pyyaml>=6.0.3",
    "redis>=7.1.0",
]
